// Math object in javascript

console.log(Math);

// Generate random number

console.log(Math.random());

// Generate random up to the given range and avoide zero in output

console.log((Math.random() * 10) +  1);

// Getting output as above code but removing decimals.

console.log(Math.floor(Math.random() * 10) +  1);

// Generate random from the given range upto the given range and avoide zero in output removing decimals.

    // defined range of output
    min = 9;
    max = 18;

    // Formula 
    console.log(Math.floor(Math.random() * (max - min)) + min);




